#pragma once

#ifndef CONSTANTS_H
#define CONSTANTS_H

class Constants {
	
	public:
		unsigned short const static MAX_ITER = 10000;
		unsigned short const static WIDTH = 1920;
		unsigned short const static HEIGHT = 1080;

		unsigned short const static HALF_HEIGHT = HEIGHT / 2;
		unsigned short const static HALF_WIDTH = WIDTH / 2;
};

#endif